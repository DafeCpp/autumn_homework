Graph(...) задаёт граф;
AddEdge(...) добавляет рёбра;
Node(...) меняет цвет/состояние вершины;
Value(...) показывает значение возле вершины;
Step(...) создаёт отдельный кадр;
Finish() завершает запись.
